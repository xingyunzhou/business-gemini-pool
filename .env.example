# Business Gemini Pool - 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================
# 基础配置
# ============================================

# 管理员密码（必需，用于 Web 登录和 API 认证）
ADMIN_PASSWORD=admin

# Deno KV 数据库路径（Docker 中会自动设置为 /data/kv.db）
# DENO_KV_PATH=/data/kv.db

# 应用端口（默认 8000）
# PORT=8000

# ============================================
# 代理配置（可选）
# ============================================

# HTTP 代理地址（用于访问 Google API）
# PROXY=http://your-proxy-server:port
# PROXY=socks5://your-proxy-server:port

# ============================================
# cfbed 上传服务配置（可选）
# ============================================

# cfbed 上传端点
# UPLOAD_ENDPOINT=https://cfbed.sanyue.de/upload

# cfbed API Token
# UPLOAD_API_TOKEN=your-cfbed-api-token

# 图片访问基础 URL（可选，默认从 UPLOAD_ENDPOINT 推断）
# IMAGE_BASE_URL=https://cfbed.sanyue.de

# ============================================
# 注意事项
# ============================================
# 1. Docker 部署时，此文件会被 docker-compose.yml 自动加载
# 2. 本地开发时，main.ts 会自动加载此文件
# 3. 不要将 .env 文件提交到 Git 仓库
# 4. 生产环境务必修改 ADMIN_PASSWORD 为强密码
